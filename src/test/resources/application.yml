spring:
  datasource:
    url: ${DB_URL:jdbc:tc:postgresql:9.6.8:test}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:test}
    password: ${DB_PASSWORD:test}
  liquibase:
    url: ${DB_URL:test}
    enabled: true
    user: test
    password: test
  messages:
    basename: i18n/messages